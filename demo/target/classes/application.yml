#配置数据源
spring:
  datasource:
     url: jdbc:mysql://127.0.0.1:3306/platform?useUnicode=true&characterEncoding=utf8
     username: root
     password: rice1004
     driver-class-name: com.mysql.jdbc.Driver
## Mybatis 配置
mybatis:
  mapper-locations: classpath:mapper/*.xml,classpath:toolmapper/*.xml
  type-aliases-package: com.example.demo.domain
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
